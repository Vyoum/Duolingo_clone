fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
sqlalchemy[asyncio]>=2.0,<3
asyncpg>=0.29,<1
alembic>=1.13,<2
pydantic>=2.0,<3
pydantic-settings>=2.0,<3
# Install shared contracts separately for local dev:
#   pip install -e ../shared
# Docker installs shared from the monorepo COPY path (see Dockerfile).
